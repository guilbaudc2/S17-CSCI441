package constants;

public class UniversalConstants {
	public static String htmlHead (String title) {
		return("<!DOCTYPE html>"
		+ "<html>"
		+ "<head>"
		+ "<title>" + title + "</title>"
		+ "</head>"
		+ "<body>");
		}
		public static String htmlClose ="<!DOCTYPE html>"
		+ "</body>"
		+ "</html>";
}
